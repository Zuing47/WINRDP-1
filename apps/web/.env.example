# URL base da API NestJS (usada quando NEXT_PUBLIC_USE_MOCKS=false)
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1

# Modo demo: "true" (default) responde tudo com dados mock em memória,
# sem necessidade de backend. Defina "false" para consumir a API real.
NEXT_PUBLIC_USE_MOCKS=true
